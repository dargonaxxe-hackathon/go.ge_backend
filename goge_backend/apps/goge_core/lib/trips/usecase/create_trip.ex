defmodule GoGe.Core.Trip.UseCase.CreateTrip do
  @moduledoc false

  def execute(driver_id, route, departure_datetime, capacity) do
    #    params = %{
    #      user_id: driver_id,
    #      departure_id: departure_id,
    #      destination_id: destination_id,
    #      departure_datetime: departure_datetime,
    #      capacity: capacity
    #    }
    #
    #    GoGe.Core.Trip.changeset(%GoGe.Core.Trip{}, params)
    #    |> @repo.insert()
  end
end
